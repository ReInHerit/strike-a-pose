export class Config {
  static SERVER_URL = `${window.location.protocol}//${window.location.hostname}:${window.location.port}/`;
  static WIDTH = 1024;
  static HEIGHT = 1024;
  static FRAME_RATE = 14;
  static VIDEO_SECONDS = 3;
  static MATCH_LEVEL = 92;
  static DEBUG = true;
  static TIME_LIMIT = "00:25:00";
  static MAX_PICTURES_SOLO = 5;
}
